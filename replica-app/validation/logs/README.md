# Validation logs

`run-full-validation.ps1` writes a timestamped JSON execution log here in addition to the stable report under `validation/reports`.

Generated Gradle HTML reports remain under `app/build/reports`; the final coverage report links to them.
